{
  "name": "Tailoring Website",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:all": "npm run build:frontend && npm run build:admin && npm run build:backend",
    "build:backend": "cd backend && npm install --only=production",
    "deploy": "npm run build:all && npm run start"
  },
  "private": true,
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}
